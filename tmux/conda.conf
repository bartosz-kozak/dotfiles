%hidden MODULE_NAME="conda"
set -g "@catppuccin_${MODULE_NAME}_icon" " "
set -gF "@catppuccin_${MODULE_NAME}_color" "#{E:@thm_peach}"
set -g "@catppuccin_${MODULE_NAME}_text" " #{CONDA_DEFAULT_ENV}"
source -F "#{d:current_file}/plugins/tmux/utils/status_module.conf"
